# function to be imported into code.py

def divide(dividend, divisior):
    return dividend / divisior

print('my_module.py: ', __name__) #special variable which tells how the code is being run. e.g. __main__ if run as stand-alone otherwise program name, i.e. my_module
